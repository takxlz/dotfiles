" ==================== gvimrc ====================
" グローバル変数の定義と、_rc-loader.vimのロードのみ行う

" _rc-loader.vimで、gvimとvimの設定を振り分けるのに使用
let g:loaded_gvimrc = 1

" g:vim_dir/rc/_rc-loader.vimをロードする
execute 'source' g:vim_dir . '/rc/_rc-loader.vim'


" gvimrcを使用するとき（現在はgui_runningを使用している）は、以下の設定を_rc-loaderのvimだけの設定の前に記述する

" gvimのときだけ、以下を実行(最後にfinishし、endif以下の設定を再度読み込まないようにする)
" (※ endif以下は、vimrc -> _rc-loader.vimのときに既に読み込まれている)
"if exists('g:loaded_gvimrc') && g:loaded_gvimrc
    "call s:source_rc('gui.rc.vim')
    "finish
"endif